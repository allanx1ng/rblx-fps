-- Map registry — add entries here for multi-map support.
-- Each key is the map name used in code; the table describes how to find it.
--
-- folder:          child name under Workspace.Maps
-- spawnPointName:  name of the SpawnLocation inside that folder

return {
	Default = {
		folder = "Default",
		spawnPointName = "MapSpawn",
	},
}
